import { Request, Response } from 'express';
import router from '../routes';

//18° ciramos nossa segunda rota e já exportamos para uso externo
export const search = (req: Request, res: Response) => {
  // res.render('page/search');
};
